%article
	%h2 What is it?
	%p Example datasets for use in API examples, etc.
	%h2 How do I use it?
	%p REST interface
	%pre http://api-example.com/[dataset][.format]
	%p e.g. http://api-example.com/employees.xml

	%h2 Datasets
	%ul
		%li 
			%a{:href => '/employees'} Employees
			(
			%a{:href => '/employees.js'} JS
			%a{:href => '/employees.json'} JSON
			%a{:href => '/employees.xml'} XML
			)

	%h2 Formats
	%ul
		%li XML
		%li JS (with callback)
		%li JSON
		%li CSV
		%li TSV
		%li WDDX


